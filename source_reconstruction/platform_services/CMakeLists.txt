add_library(th20_platform_services STATIC configuration.cpp clock.cpp services.cpp)
target_link_libraries(th20_platform_services PUBLIC th20_runtime_core winmm)
add_library(th20_platform_entry_adapter STATIC entry_adapter.cpp)
target_link_libraries(th20_platform_entry_adapter PUBLIC th20_platform_services)
target_compile_options(th20_platform_entry_adapter PRIVATE /W4 /utf-8)
target_include_directories(th20_platform_services PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_compile_features(th20_platform_services PUBLIC cxx_std_17)
target_compile_options(th20_platform_services PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
if(BUILD_TESTING)
  add_executable(th20_platform_services_tests services_tests.cpp)
  target_compile_options(th20_platform_services_tests PRIVATE /W4 /utf-8)
  target_link_libraries(th20_platform_services_tests PRIVATE th20_platform_services)
  add_test(NAME th20_platform_services_tests COMMAND th20_platform_services_tests "${CMAKE_CURRENT_BINARY_DIR}/isolated_service_tests")
endif()
if(TH20_BUILD_ORACLE)
  add_executable(th20_platform_cpu_compare cpu_compare.cpp ../../src/binary.cpp ../../src/pe.cpp)
  target_include_directories(th20_platform_cpu_compare PRIVATE ../../native_recovered ../../include)
  target_compile_options(th20_platform_cpu_compare PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
  target_compile_definitions(th20_platform_cpu_compare PRIVATE TH20_NATIVE_CORE_SHA256="not-used-by-platform")
  foreach(source configuration.cpp configuration.hpp clock.cpp clock.hpp)
    file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/${source}" digest)
    string(REPLACE "." "_" symbol "${source}")
    target_compile_definitions(th20_platform_cpu_compare PRIVATE "TH20_SHA_${symbol}=\"${digest}\"")
    set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS ${source})
  endforeach()
  target_link_options(th20_platform_cpu_compare PRIVATE /BASE:0x10000000 /DYNAMICBASE:NO /NXCOMPAT)
  target_link_libraries(th20_platform_cpu_compare PRIVATE th20_platform_services bcrypt)
endif()
