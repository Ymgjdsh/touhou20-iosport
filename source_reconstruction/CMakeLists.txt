cmake_minimum_required(VERSION 3.16)
cmake_policy(SET CMP0091 NEW)
project(th20_independent_source_modules LANGUAGES CXX)
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
include(CTest)
add_subdirectory(archive)
add_subdirectory(core_scheduler)
add_subdirectory(runtime_core)
add_subdirectory(platform_services)
add_subdirectory(input)
add_subdirectory(program_entry)
add_subdirectory(platform_window)
add_subdirectory(ecl_vm)
add_subdirectory(runtime_state)
add_subdirectory(game_session)
add_subdirectory(sprite_renderer)
add_library(th20_resource_manager STATIC archive/resource_manager.cpp archive/manager_entry_adapter.cpp)
target_link_libraries(th20_resource_manager PUBLIC th20_source_archive th20_platform_services)
target_compile_options(th20_resource_manager PRIVATE /W4 /utf-8)
add_executable(th20_resource_manager_tests archive/manager_tests.cpp)
target_link_libraries(th20_resource_manager_tests PRIVATE th20_resource_manager)
target_compile_options(th20_resource_manager_tests PRIVATE /W4 /utf-8)
file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/archive/resource_manager.cpp" TH20_MANAGER_CPP_SHA)
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS archive/resource_manager.cpp)
target_compile_definitions(th20_resource_manager_tests PRIVATE TH20_MANAGER_CPP_SHA="${TH20_MANAGER_CPP_SHA}")
add_subdirectory(audio_runtime)
add_subdirectory(progress_state)
add_subdirectory(gameplay)
add_subdirectory(text_renderer)
add_subdirectory(screen_effect)
add_subdirectory(stage_background)
add_subdirectory(damage_regions)
add_subdirectory(player_entity)
add_subdirectory(bomb_system)
add_subdirectory(effect_system)
add_subdirectory(item_system)
add_subdirectory(small_score)
add_subdirectory(stone_menu)
add_subdirectory(special_state)
add_subdirectory(overlay_system)
add_subdirectory(hud_system)
add_subdirectory(stage_clear)
add_subdirectory(stage_completion)
add_subdirectory(card_system)
add_subdirectory(replay_system)
add_subdirectory(pause_system)
add_subdirectory(trophy_system)
add_subdirectory(help_system)
add_subdirectory(notice_system)
add_subdirectory(options_system)
add_subdirectory(key_config)
add_subdirectory(ending_scene)
add_subdirectory(title_system)
add_subdirectory(bullet_system)
add_subdirectory(laser_system)
add_subdirectory(startup_scene)
add_subdirectory(link_probe)
add_subdirectory(diagnostics)
option(TH20_SOURCE_ASAN "Instrument the independent source build for memory diagnostics" OFF)
if(TH20_SOURCE_ASAN)
  # This SDK installation supplies the dynamic VC ASAN support library. Keep
  # the ordinary /MT production build unchanged and use /MD only for diagnosis.
  function(th20_instrument_directory directory)
    get_property(targets DIRECTORY "${directory}" PROPERTY BUILDSYSTEM_TARGETS)
    foreach(target IN LISTS targets)
      get_target_property(type "${target}" TYPE)
      if(NOT type STREQUAL "INTERFACE_LIBRARY" AND NOT type STREQUAL "UTILITY")
        set_property(TARGET "${target}" PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreadedDLL")
        target_compile_options("${target}" PRIVATE /fsanitize=address /Zi /Oy-)
        if(type STREQUAL "EXECUTABLE" OR type STREQUAL "SHARED_LIBRARY")
          target_link_options("${target}" PRIVATE /INCREMENTAL:NO /DEBUG)
        endif()
      endif()
    endforeach()
    get_property(children DIRECTORY "${directory}" PROPERTY SUBDIRECTORIES)
    foreach(child IN LISTS children)
      th20_instrument_directory("${child}")
    endforeach()
  endfunction()
  th20_instrument_directory("${CMAKE_CURRENT_SOURCE_DIR}")
endif()
# th20_source is an explicit integration target. Successful linking alone is
# not full-game equivalence; per-module and end-to-end checks remain separate.
