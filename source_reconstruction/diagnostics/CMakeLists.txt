add_executable(th20_source_debugger source_debugger.cpp)
target_compile_features(th20_source_debugger PRIVATE cxx_std_20)
target_compile_options(th20_source_debugger PRIVATE /W4 /utf-8)
target_link_libraries(th20_source_debugger PRIVATE dbghelp)

find_path(TH20_DIA_INCLUDE dia2.h HINTS "$ENV{VSINSTALLDIR}/DIA SDK/include" "D:/VS2019/IDE/DIA SDK/include")
if(TH20_DIA_INCLUDE)
  get_filename_component(TH20_DIA_ROOT "${TH20_DIA_INCLUDE}" DIRECTORY)
  add_executable(th20_pdb_lookup pdb_lookup.cpp)
  target_include_directories(th20_pdb_lookup PRIVATE "${TH20_DIA_INCLUDE}")
  target_compile_definitions(th20_pdb_lookup PRIVATE "TH20_DIA_DLL=L\"${TH20_DIA_ROOT}/bin/msdia140.dll\"")
  target_compile_options(th20_pdb_lookup PRIVATE /W4 /utf-8)
  target_link_libraries(th20_pdb_lookup PRIVATE ole32 oleaut32 "${TH20_DIA_ROOT}/lib/diaguids.lib")
endif()
