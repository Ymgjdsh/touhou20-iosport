# PlayerInf source reconstruction

The original owner is `0x1485c` bytes, allocated by `4f32b0`, constructed by `4f46a0`, initialized by `4f9520` and published at Context `+4` by `4ffff0`. `owner.hpp` now defines that complete prefix as an actual `runtime::CallbackOwner` subclass with a source-only services pointer suffix. Production never executes original instructions. Scalar collision functions also accept non-owning views for reuse by Bullet, Enemy and Bomb code.

The complete constructor, destructor `4f5320`, initialization, factory and `4feff0` cleanup are recovered. Layout includes the `5e4`-byte ANM, ten plus twelve `12c`-byte Options, `5c`-byte Feedback, and `12598`-byte ShotController containing 256 `124`-byte Shots and two observer-aware lists. Constructors preserve actual unwritten padding, including Player `+14850`; factory allocation is zeroed separately. The original `40c6b0` lifecycle diagnostics do nothing in this release, so no log messages are invented. Teardown unregisters callbacks, retires active shots, handles animation-file preservation and owned SHT storage, then destroys feedback and ANM resources in original order. `shots.cpp` restores `506af0/506ab0` retirement and recycling, including global-context-0 damage lookup behind the current-context guard.

`initialize.cpp` restores `4f9520`, `504bd0`, `4f9470` and the Player/Option/Shot/Feedback context selectors. Production reads actual `plNN.anm` and `plNN.sht`, binds ANM script zero, registers priorities 29/30, initializes fixed-point positions and power settings, and creates the damage owner. `shot_data.cpp` restores the signed SHT offset table at `+5d4`, count16 at `+2`: nonnegative offsets become source-owned pointers, negative sentinels survive. It rejects malformed bounds before mutation; equivalent behavior is claimed for valid resources. The retained-mode `5c60f0` path preserves the original assignment after a new resource read, including the original displaced-pointer ownership quirk. Load failures also preserve the original temporarily published context pointer after factory destruction. These source behaviors are documented rather than silently corrected.

`collision.cpp` restores `460850` position (`+614`), `4ff350` angle to the player, `4ff5e0` focus byte (`+204c`), `4ff800` expanded-collision flag (`+14` bit 4), and `4ff490` current Player `+e8` signed clamp. The angle returns exactly positive pi/2 for a zero x/y difference. Context is at `+14858`; current Player is Context `+24`.

Three complete collision entry sequences are restored:

- `4f8ce0`, axis-aligned overlap using the selected focus extent (`+209c/+20a8`) and a separate 48-by-48 graze region.
- `4f8ff0`, circular overlap using the sum of squared radii. Its two COMISS/JB decisions include unordered IEEE inputs. Focus selects radius `+2094/+2098`; current Player collision percentage mutates through its actual clamp. Expanded collision uses `+2238 * 3.6 * radius`. Graze radius is `max(radius / 2.5, 40)` with the original comparison behavior.
- `4f91d0`, rotated rectangle collision using the recovered `geometry::rectangle_circle` (`457610`) and the original parameter order. It omits the expanded-radius multiplier, uses base radius plus 30 for graze, and returns zero for invulnerable contact. Axis-aligned/circular contact instead returns one while invulnerable.

The production adapter supplies the real nullable boss HUD and the recovered hit source. It also binds Enemy damage's circle/rectangle interfaces. `events.cpp` restores hit `4f86f0` and graze `4f8b90` control flow. Actual adapters use shared Enemy/Bomb notifications, SoundInf, EffectInf, ANM script binding, stream 0 RNG, Item counters and damage rewards. Hit changes overlay mode or player state/timers in the original order. Graze increments Player `+e4` with the original wrapping add/clamp, enqueues type 4 with one RNG draw modulo 4, forces only the color alpha byte, then applies rewards and sound.

`power.cpp` restores complete `4faca0` control flow and `4fb290/4ffd80/4fee50/4f58f0`, with 128-scale fixed-point coordinates, modulo-32 addition and CVTTSS2SI conversion. It preserves the discarded negative-argument getter, maximum-power animations, global versus current focus queries, offset callbacks and option state ordering. Item's power refresh and gameplay's `finish_entity_initialization` bind to this source. `draw_player` restores `4f8af0` using the actual source renderer. The complete Player main frame `4f7430` is now in `frame.cpp`. Overlay `534130/533180/4ff760/4ff630` virtual behavior and shared special-owner getters are supplied by their recovered owning modules. These are not replaced by successful empty bodies.

`firing.cpp` restores `504d40/505d90/504e90`: signed SHT period/phase tests, type-2 existing-shot reuse, all shot initialization fields, option fixed positions, damage bonuses, bounded angle wrapping and RNG order, ANM orientation/scale, DamageRegion callbacks and sound. `506210/505fc0` implement the pool, overflow allocation and wrapping handle; `506480/5060a0/5062f0/506310` implement actual relocated records and Option views. Overflow initially selects global Context 0 even when the requesting controller is Context 1; final initialization then selects the requested view. Shot ANM comes from **Player +24**, distinct from the base animation file at +1c. Production adapters call source ANM, DamageRegion, allocator, RNG and SoundInf. Shot callback resolution binds recovered initialization/update adapters; all initialization, update and hit callbacks are independently compiled C++; the complete Player main frame is connected through the source frame adapter.

`cpu_validation.json` contains **1,029,407 original CPU checks and 2,754 source assertions**, all passed. The functions under comparison retain their original bodies. The new main-frame group uses explicitly recorded external endpoints, as detailed below; the other groups keep their stated native service boundaries. Full constructor and ShotController initialization comparisons retain all original bytes and self-relative list anchors; only the original Player vptr/base bytes are normalized to the independently recovered base. Other CPU cases cover context selectors, fixed-point operations, all power clamps, zero-level complete `4faca0`, scalar accessors and collisions. Scalar collision data includes finite and negative dimensions, zero, infinity, NaN and subnormal values; rotation and atan2 inputs are finite. Nonfinite trig tests terminated the isolated mapped-image process and are excluded. Positive invulnerability avoids executing the original hit graph.

The firing additions include 69,200 CPU comparisons of records, options, pool allocation and complete `504d40` with original nested `505d90` existing-shot reuse. Another 9,600 comparisons exercise complete `504e90` with identical unmodified native **ANM allocation/VM, DamageRegion allocation and RNG service boundaries** supplied to both implementations. They compare the whole Player object, PlayerTable mutation, entire ANM pool slot, damage owner and RNG state. This isolates the initializer and does not independently certify those boundary implementations. The initializer domain covers source 0..6, types 0..7, orientation 0..7, both contexts and finite angles beyond the wrap limit; callback indices are zero and sound is disabled by the real -1 sentinel. Source assertions separately cover callback rejection before sound, type-2 registry publication and genuine heap overflow allocation.

`shot_callbacks.cpp` restores every nonnull initialization-table index 1..31 at `573178`: the `500270..5017c0` family, repeated function entries and the original `414b50` empty callback. The independent `504a60` arithmetic preserves the unusual `modulus/(2*pi)-pi` denominator. The production table stores C++ fastcall adapters compatible with the original thiscall stack cleanup. It has no original code addresses. The complete update table at `573200` (indices 1..16) is also restored. The extra table at `5731fc` has one valid null entry; invalid out-of-bounds indices are rejected. All ten nonnull hit indices at `573248` are restored, including the index4/index10 alias. The final Shot word at +4c is a **hit callback**, consumed directly by DamageRegion; it is not a destructor.

The initialization callbacks add 217,000 checks across all 31 entries, including complete Player, RNG, DamageRegion/SoundInf and ANM state. Native readonly enemy queries, RNG draw, sound queue and handle lookup are shared service boundaries in this oracle. Tests retain list order, exclusion flags, missing targets, attachment fixed positions, current/global contexts, and finite out-of-range angles. Initializer 24 preserves the original `450670` behavior: write ANM scale +50/+54 without marking dirty flags. Angle operations that require original intermediate float rounding use explicit single-precision arithmetic.

Source tests separately verify full hit/graze/power/retirement ordering, factory success/failure and teardown with recorded required ANM, HUD and damage boundaries. Both real archive SHT files are loaded; all 320 relocation entries and unchanged payloads are checked. The report records resource and source/test/dependency hashes. This does not assert complete original lifecycle execution, GPU uploads, higher-level original power animation branches or whole-game equivalence.

```powershell
& D:\cmake\bin\cmake.exe -S source_reconstruction/player_entity -B source_reconstruction/player_entity/build -G 'Visual Studio 16 2019' -A Win32
& D:\cmake\bin\cmake.exe --build source_reconstruction/player_entity/build --config Release --target th20_player_entity_cpu_compare
& source_reconstruction/player_entity/build/Release/th20_player_entity_cpu_compare.exe '<verified th20.exe>' source_reconstruction/player_entity/cpu_validation.json
```

Link production target `th20_player_entity`; include `player.hpp` and use namespace `th20::source::player_entity`. Default collision overloads use production services; explicit `CollisionServices&` overloads isolate device/owner effects for testing. No test original-image mapping code is linked into the production library.

The 16 update entries add 156,800 original CPU checks: tracking, acceleration, horizontal retargeting, reflection, both continuous lasers, normalized growing-beam placement and timed cancellation. All original Player bytes, RNG, DamageRegion, ANM root/child, SoundInf and DirectSound SetPan calls are compared. The two continuous lasers intentionally differ in whether row offsets affect the damage center, whether damage/angle are refreshed, and their weapon/player/dialogue termination tests. `502db0` normalizes Motion +38 before placing the damage center; `502f70` sends rectangle cancellation to Context +0 Bullet and +14 Laser (getter `41ca90`). Native readonly queries, damage allocation/retirement, sound/device endpoints, ANM lookup/interrupt and empty-list cancellation remain shared oracle boundaries; this does not certify their implementations or whole-frame behavior. Production adapters bind source implementations.

`shot_hit.cpp` restores `5032d0/503450/503ad0/503b70/504180/503d40/503ee0/503fc0/504160` and the original duplicate table entry. Hit callbacks keep the original 16-byte stack-argument convention, ANM colors/interpolation, RNG stream 0 versus 1 and four DamageRegion lookup/retirement ordering differences. In `503b70`, copying Shot Motion into the retired old region is deliberate. The full hit groups contribute 81,200 CPU checks: 44,800 damage-body, 33,600 effect-body and 2,800 effect-handle registry checks. Native ANM spawn/VM, default-hit body, DamageRegion creation/retirement and sound services are common boundaries; complete caller and affected object bytes are compared. Effect tracking `497d20/498e70` uses +20, aliasing EffectInf.files[4], and returns the old cursor while testing the new cursor; there is no second shadow storage. Its oracle uses valid cursor0..1023 and bounded ANM handle indices.

`shot_geometry.cpp` restores `456170/4545d0/454480/4547e0` with existing source `geometry::segment_segment` (`456920`), adding 36,001 CPU comparisons. The original tests the ray angle before applying the rectangle rotation. Collinear horizontal edges can generate NaN intersection coordinates; the original COMISS/JBE distance ordering selects the second intersection for unordered inputs, which the source preserves. Finite geometric inputs, zero and negative dimensions, vertical/collinear segments and unchanged output z coordinates are included.

`stage_reset.cpp` restores complete stage reset `4fb450`, Option animation state `4ff8f0/4bd0c0` and secondary-player disabling `4ffd20`. It reuses real source Shot clearing, power refresh and ANM services, resets all evidenced fields and SHT speeds, copies the raw +620 fixed coordinates into all33 history entries, clamps actual Session PlayerTable +1e4 and preserves the conditional player1 hiding. Another 12,000 CPU checks compare the entire Player, ANM requests and shared Session across2,400 scenarios; the full original power refresh is a shared boundary here, with zero-power player records. The main frame now calls this source reset and visibility implementation.

`frame_helpers.cpp` restores fixed-point movement `4ffa10`, feedback update `4f7210`, Shot update `504480` and viewport test `506570`. Another 26,400 CPU checks compare complete Player, ANM and damage fields, finite motion inputs, fractional/signed clock rates, timer expiration, retirement and every orientation. The viewport predicate preserves all four COMISS/JAE unordered branches. Orientation5 uses `angle_difference` before two bounded wraps. The single-Shot tests share native ANM corner, retirement and one callback boundary; geometry extraction uses the original no-output ANM type4 branch. The callback bodies themselves are independently covered above.

`shot_controller_frame.cpp` restores `504210/505a30`, including actual input words +29c, input-slot mapping +2e30, gates, the14/119 frame cycles, power-clamping getters, Overlay virtual dispatch, observer-safe iteration and counts that include retired shots. Its 38,400 CPU checks compare complete Player, PlayerTable, ANM and recorded weapon calls. Gate getters, weapon virtual endpoints and ANM retirement are explicit shared test boundaries. Production uses source Input, StageClear, Game/Enemy/HUD, Overlay and audio objects.

`option_frame.cpp` restores complete `4fa860` (ECX=Player, stack argument=Option). Its20,800 CPU checks cover focus switching, SHT animation replacement, interrupt7 followed by VM execution, fixed-point overflow, smoothing thresholds, disappearance and both optional thiscall strategy endpoints. The whole Player, actual four-slot ANM fixture, lists, generations and PlayerTable are compared. ANM spawn/VM and weapon strategy endpoints are shared test services; source production binds their owning modules.

`movement.cpp` restores `4f9c30` and `4fff10`: directional input priority and table572c48, slow movement, focus-effect state, screen-side transition, turn animation, raw fixed-point position history and all22 Option updates. Another19,200 CPU checks compare the whole Player and affected ANM, PlayerTable, sounds and generation counters with one live Option and recorded/native resource endpoints. The main-state dispatch and death handling below reuse this source movement. None of these local results establish whole-game equivalence.
`death_state.cpp` restores `4f8420/4f86c0`, power subtraction `4e1680`, death and stone counters `4fe950/4fea60/4fe9b0/4feab0/4fea00/4feb00`, and effect reservation/spawn `4fb820`. Another31,200 CPU checks cover wrapping integer inputs, all clamping limits, full Player/Session/Enemy/Card/Effect changes and real four-slot ANM allocation. Effect reservations occur before allocation and preserve the original shared cursor. The standalone death group uses original lives/meter/Card and ANM services, absent HUD and an empty Enemy list. Production calls the actual Item, Bomb/Card and effect modules.

`frame.cpp` restores the complete `4f7430` state dispatch and tail: initial entry, normal movement, hit window, death, revival, directional movement, hidden state, both countdowns, color flashing, expansion interpolation, ShotController and Feedback update. `frame_adapter.cpp` connects the actual source Input, Bomb, Bullet, Laser, Damage, Item, Replay, Pause, HUD, clock and ANM modules. There is no native call in production. The original game-over branch advances the main timer twice; the hidden state stores literal480 in fixed Y; both behaviors are retained.

The main-frame group adds33,600 CPU checks over5,600 scenarios (states0..7,17 boundary frame values including negative1, fractional rates0..2, all four power levels, expansion modes0..17, bomb availability/input and game-over/replay decisions). It compares every Player byte, Session/Enemy/Card/Effect changes, real ANM slots, generation, sound/weapon activity, clock rate and the ordered external call arguments. The mapped original `4f7430`, Movement, Death, ShotController and Feedback bodies remain unmodified. For this group alone, external Bullet/Laser cancellation, Damage allocation/activation, Item creation, Bomb eligibility/trigger, Pause finish and HUD lives/bombs notifications are redirected to explicit trace endpoints in the isolated oracle image and restored afterwards. ANM execution and scalar interpolation remain native shared services. Shots/Options start inactive in this group; their active branches have separate coverage above. Thus the result validates dispatcher behavior and its composition with the independently compared lower bodies, not resource endpoint implementation or complete gameplay equivalence.