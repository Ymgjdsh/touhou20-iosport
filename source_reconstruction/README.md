# 完整独立 C++ 源码恢复工程（未完成）

用户要求完整源码。此前 `dist/playable/th20_rebuilt.exe` 保留了大部分原版机器码，不满足该要求，不能作为本工程完成的证据。

这里的交付标准是：游戏逻辑均从可编辑源码编译，运行时不启动、不映射执行、不内嵌原 EXE 机器码；允许读取原版资源和关卡数据，调用标准系统/图形 API。原作者丢失的注释与局部变量名无法从机器码直接取回，恢复目标是行为等价、可编译、可维护的源码。

**整套源码已成功链接成独立 EXE，完整游戏验收仍未完成。** `../build_source_game.ps1` 构建并打包 `../dist/source_game/th20_source.exe`；已修复加载时回调过早启用及敌机释放后访问，实际运行第 1、3 关演示和非演示游戏。构建不使用原 EXE，原机器码对照仅存在于独立测试工具中。构建入口见 [SOURCE_BUILD.md](../SOURCE_BUILD.md)，实际验证范围见 [源码版运行记录](../reports/SOURCE_GAME_RUNTIME.md)。

## 当前源码

| 目录 | 实际内容 | 验证边界（仅对应报告记录的源码版本） |
|---|---|---|
| `archive/` | THA1/LZSS 解码、进程资源管理器、共享字典与锁、归档及显式松散文件读取 | 285 条资源逐字节对照；872 项管理器集成检查。源码采用独立所有权 API；损坏归档的原版越界行为不在等价范围 |
| `core_scheduler/` | 回调节点、可删除迭代器、优先级链表、更新/绘制调度及生产适配 | 13,768 项原 CPU 对照，比较对象及回调顺序；跨线程竞争、异常展开与完整渲染调度另需验收 |
| `runtime_core/` | 分配器、PMR、22 个递归锁、日志、回调对象与工作线程 | 2,541 项局部 CPU 对照及线程生命周期测试；未证明任意并发时序一致 |
| `runtime_state/` | 四个 RNG 全局对象、种子与浮点随机采样、时钟缩放、共享 Motion | RNG 164,000 项、Motion 41,984 项原 CPU 对照，覆盖舍入、角度循环和量化边界；整局 RNG 调用顺序还需回放验证 |
| `platform_services/`、`input/` | 配置读写、时钟、Win32/DirectInput/XInput/WMI 输入枚举与状态处理 | 平台 66,270 项、输入 44,271 项 CPU 对照；硬件/驱动差异和实际输入到游戏帧的同步需运行验证 |
| `program_entry/`、`platform_window/` | WinMain、三个主循环、窗口/设置对话框、全局构造、D3D9 设备、视口/摄像机、截图、场景切换与帧统计 | 窗口布局/查询等 30,867 项 CPU 对照，设置对话框资源逐字节一致；设备恢复、消息循环与场景切换的完整组合以运行记录为准 |
| `audio_runtime/` | WAV 解析、90 个 DirectSound 效果通道、BGM 命令/流、通知线程、资源与进程适配 | 原 CPU 调用/状态对照及真实 DirectSound 后端测试；驱动时序、实际输出声波和全游戏音频同步未完成验收 |
| `ecl_vm/` | 通用 ECL 指令、同步/异步脚本、栈、计时、数学与插值；由 Enemy 适配器接入实体状态 | 通用执行器 41,067 项 CPU 对照；实体专用指令另见 `gameplay/` 报告，不能将通用 VM 测试当作整关脚本验收 |
| `sprite_renderer/` | ANM VM、65,536 项动画池、层调度、文件预载/后载、贴图、49 种绘制分支与管理器生命周期 | ANM、池及绘制有分项 CPU 报告；73 个 ANM 预载、2,181 组真实 D3D9 贴图及 469,066,224 像素对照。共享池报告含多个调用模块，不可重复加总；整帧显示仍需验收 |
| `game_session/`、`gameplay/` | 会话和 GameController；Enemy 存储、生成、资源加载、ECL 分派与实体指令、运动、受伤/阶段、网格、掉落和递归死亡处理 | 会话 14,342 项、Enemy 1,254,134 项 CPU 对照，另有真实归档资源与帧顺序源码测试；部分重型指令/资源终点采用记录边界，尚非全实体整局逐帧比较 |
| `player_entity/` | Player/Option/Shot 存储、SHT 读取、射击及全部初始化/更新/命中回调、移动、碰撞、擦弹、火力、死亡/复活和完整主帧分派 | 1,029,407 项 CPU 对照及 2,754 项源码断言；主帧外部消弹、道具、Bomb、HUD 等使用明确测试边界，活动 Shot/Option 分组验证；完整实战组合尚待回放验收 |
| `text_renderer/` | ASCII/CP932 排版、字图集、格式化、GDI 栅格化、动态文字缓存/克隆、延迟上传与绘制队列 | 报告记录 57,113 项检查（含菜单共用组），覆盖 22 种字体分支；上传像素使用内存 COM 观察面，未验证任意并发、完整生命周期或全游戏 GPU 显示 |
| `stage_background/` | STD 资源与场景 VM、插值、背景对象、摄像机、雾、网格和更新/绘制 | 状态 62,048 项、VM 43,080 项，另有共享 ANM/对象绘制对照；完整资源工厂、过渡特效及全关卡组合需端到端验证 |
| `progress_state/` | 分数/符卡记录、存档快照、压缩/加密、备份恢复、线程读写与解锁数据 | 99,312 项原 CPU 检查，包含整文件编解码、解锁及 RNG；源码工作线程执行隔离文件读写。原线程启动 ABI、任意竞争与磁盘满场景不在已验证范围 |
| `screen_effect/` | 十种屏幕效果的对象、状态推进、生命周期与绘制 | 124,288 项 CPU 对照；效果叠加到实际场景后的完整画面仍需验收 |
| `bomb_system/` | 两角色 Bomb、状态/运动、伤害及消弹生产调用 | 状态 68,387 项，另有角色/完整对象与 9,216 项实际 Bullet 消弹组合检查；后者使用空 Laser 管理器，不证明非空激光和道具生成组合 |
| `damage_regions/` | 256 项伤害区域池、五类几何、伤害聚合、击中回调、得分与物品奖励 | 296,121 项 CPU 对照；道具创建与部分回调是受控测试终点，生产连接实际模块；完整碰撞/得分帧仍需整局比较 |
| `effect_system/` | EffectInf 管理器和 15 种具体效果，包括轨迹、粒子、过渡面板与石选择特效 | 各类型有原 CPU 对照及输入/哈希记录；资源分配、完整生命周期和全场景组合按各报告限定，不由类型齐全推定 1:1 |
| `item_system/`、`small_score/` | 1,536 项物品池、生成/运动/拾取收益、特殊物品与飘字的更新/绘制/生命周期 | Item 287,824 项、飘字 46,208 项 CPU 对照；ANM、HUD、特效等终点部分记录调用，原版池耗尽/损坏输入不在比较域 |
| `stone_menu/` | StoneMenuInf、游标、文字解析、初始化、更新/绘制及工厂/生命周期；接入共享特殊状态和武器覆盖层 | 110,128 项 CPU 检查；资源、动画、音频与部分存档/覆盖层查询采用记录边界，未证明整个菜单的线程和 GPU 组合 |
| `special_state/`、`overlay_system/` | 异变石共享状态、WeaponStoneInf、两角色 18 个工厂位置及对应具体武器；实际 Replay 继承石读取已接通 | Special 116,736 项、Overlay 870,400 项对照；整所有者创建/选择、网格、分配及并发组合尚未全部原 CPU 验证，测试终点范围见各报告 |
| `hud_system/` | HUD、分数缓动、图标/通知、对话脚本/文字/立绘及生产更新/绘制 | HUD 报告 232,192 项对照，覆盖排队文字和受限启用/帧路径；活动对话推进/删除及完整所有者生命周期不在该组验证范围 |
| `stage_clear/` | 关卡结算计分、状态机、动画、文字与生命/阶段字段操作 | 95,232 项原 CPU 对照；自删除并调用整关完成的状态 6 路径、资源初始化和新文字任务上传未由此组覆盖 |
| `bullet_system/` | 2,001 项子弹存储、运动/反弹/消弹/自机碰撞、50 种样式、扩展指令、发射及敌机/激光生成 | 341,623 项 CPU 对照，包括真实 Type0/1 激光生成；ETEX24、敌机资源样式选择、自机碰撞和掉落组合未纳入该池 oracle，不能据此宣称所有弹幕已验收 |
| `laser_system/` | 四类激光的管理器/工厂、阶段、运动、轨迹采样、绘制、碰撞/擦弹、消弹与分裂源码 | 431,606 项原 CPU 检查和 64 项源码检查；具体激光完整更新/绘制、Type2 消弹及部分敌机资源分支尚未覆盖。ETEX13 原版越界路径明确排除，见 `TYPE2_ETEX13.md` |
| `startup_scene/` | 加载场景、后台资源读取、启动动画、文字与到标题场景的转场连接 | 按名称创建动画有 24,576 项对照；完整后台线程、资源就绪与场景切换链需独立 EXE 运行验证 |
| `card_system/` | 符卡开始/更新/结束、计分、捕获记录、计时与 Replay 数据互通 | 206,784 项原 CPU 对照；新标题任务的异步 GDI、敌机画像选择及全部符卡捕获成就终点未由该组覆盖 |
| `replay_system/` | 录像结构、记录/播放、快进、文件读写、输入重复、自机状态恢复与继承石查询 | 35,840 项结构/输入 CPU 对照；四段原演示共 27,411 帧经过源码加载和解码；这不是实际播放后的实体/得分/画面逐帧一致证明 |
| `stage_completion/` | 通关分派、解锁/授予、游玩时长、续关及真实 Replay 访问 | 126,976 项原 CPU 对照；UI、自机、音频采用明确终点；与 Progress 共用的辅助检查不可重复加总，完整通关流程尚待运行验收 |
| `pause_system/` | 暂停/恢复、截图背景、游标历史、菜单、排名、录像保存与绘制 | 114,688 项控制流、81,920 项绘制对照；完整截图/所有者生命周期、文件线程、新字图上传和任意菜单组合尚未覆盖 |
| `title_system/` | 标题生命周期与帧分派；主菜单、难度/角色/石配置、练习、音乐、回放、排名/保存、玩家数据与成就页面 | 状态/分派 2,540,032 项；绘制、姓名/数据页和真实音乐归档解析另有报告。GPU、异步调度及全流程另需验收；回放格式串和音乐文档的原版越界输入域明确排除 |
| `options_system/`、`key_config/` | 设置页面、音量、窗口/设备选项、键盘/手柄映射、状态更新与绘制 | Options 393,216 项、KeyConfig 229,376 项状态对照，另有共用绘制检查；硬件切换、配置文件与完整页面生命周期组合需运行验证 |
| `trophy_system/` | 成就编码/消息队列、存档标记、提示对象、动画、音效及延迟文字任务 | 编码/队列 295,327 项、所有者/帧 34,404 项；后者执行真实 GDI 并比较上传像素，COM 面为观察夹具；实际资源预载、GPU 显示和全局调度另验 |
| `help_system/` | HelpInf 生命周期、完整帧、两类后台加载任务、帮助图片与 D3D9 纹理更新 | 生产源码已接入；尚无该完整帮助页面的原 CPU 帧、线程与纹理内容差分，不能用编译或其他页面测试替代 |
| `notice_system/` | 提示页面的对象/游标、消息表、状态推进、后台读取、文字及图片资源连接 | 59,904 项原 CPU 检查；缓存命中读取同步执行，异步启动、图片替换分支和非空文件销毁未覆盖；上传像素观察不等于实际 GPU 页面验收 |
| `ending_scene/` | 结局/职员表场景、记录、脚本 VM、资源、状态推进与文字任务 | 200,618 项原 CPU 检查；异步文件加载、职员表文件替换和完整所有者销毁未覆盖，尚无通关后的完整结局回放验收 |
| `../native_recovered/` | 独立 C++ 随机数核心与计时器，供源码模块使用 | 七个组件的原机器码/实际 DLL ABI 差分已有记录；旧增量 DLL 的验证不替代当前独立 EXE 的验收 |
| `link_probe/`、`diagnostics/` | 从真实 WinMain 和源码模块链接的 EXE 目标、独立源码进程调试与崩溃记录工具 | 可证明链接和记录具体运行故障；不证明全部功能覆盖或整局 1:1，运行结果见 `../reports/SOURCE_GAME_RUNTIME.md` |
| `audit/` | 逐函数恢复状态、原样伪代码编译探针、间接调用与未知类型清单 | 审计证据不是生产实现；函数数量和成功链接都不能换算为完整游戏完成比例 |

`analysis/ghidra/pseudocode/` 的 6,928 个函数文件和合并伪代码仍只作为逆向参考。其原样 C++ 编译实际失败。`scripts/recovered/` 包含已回编验证的专用关卡/动画等脚本，不应改名冒充 C++。

## 构建

```powershell
Set-Location 'D:\AIWorkspace\Touhou20Reconstruction'
.\build_sources.ps1
```

该命令只编译独立模块及源码测试，不需要执行原游戏代码。可选的隔离原函数对照：

```powershell
.\build_sources.ps1 -Validate
```

`-Validate` 构建单独的硬件 oracle 测试工具，其只读映射原 EXE 的选定函数用于差分，不执行原游戏入口；它不是待交付游戏的组成部分。构建结果位于 `build_sources/`。另用 `../build_source_game.ps1` 可构建整套源码 EXE，但成功链接不能替代实际游戏验收。

## 仍然缺失的完整游戏实现

自机逐帧、敌机专用 ECL、死亡处理、暂停、标题菜单、录像和后续场景现已有源码实现。剩余工作是核对完整覆盖并完成跨模块和游戏运行验收；初次主菜单运行后的加载崩溃表明局部测试仍有未覆盖的组合。原版自身的非法内存访问另行记录，不以静默占位绕过。没有从模块数量推算完成百分比。`link_probe/` 使用真实 WinMain 和源码模块，链接成功不是游戏完成比例。

各模块的 `cpu_validation.json` 等报告说明实际覆盖范围和源码哈希。构造、局部算法、COM 调用序列、真实显卡像素与全游戏逐帧结果属于不同验证层次，不能相互替代。模块持续修改时，旧报告仅对应其中记录的源码版本。

独立模块的局部验证不能推出整款游戏 1:1。完整验收还需要从源码链接游戏后，在同输入/种子下比较全部实体状态、碰撞/得分、图像和音频，并覆盖角色、难度、关卡与通关流程。
