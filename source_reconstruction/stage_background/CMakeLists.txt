add_library(th20_stage_background STATIC state.cpp lifecycle.cpp fog.cpp camera_motion.cpp vm.cpp vm_adapter.cpp update.cpp mesh_distortion.cpp draw.cpp)
target_compile_features(th20_stage_background PUBLIC cxx_std_20)
target_compile_options(th20_stage_background PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
target_link_libraries(th20_stage_background PUBLIC th20_sprite_renderer th20_runtime_core th20_game_session th20_gameplay th20_platform_window th20_resource_manager th20_screen_effect)
if(TH20_BUILD_ORACLE)
  add_executable(th20_background_state_cpu_compare state_cpu_compare.cpp ../../src/binary.cpp ../../src/pe.cpp)
  target_link_libraries(th20_background_state_cpu_compare PRIVATE th20_stage_background bcrypt)
  target_include_directories(th20_background_state_cpu_compare PRIVATE ../../include ../../native_recovered)
  target_compile_options(th20_background_state_cpu_compare PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
  target_link_options(th20_background_state_cpu_compare PRIVATE /BASE:0x10000000 /DYNAMICBASE:NO)
  file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/state.cpp" BACKGROUND_STATE_SHA)
  file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/background.hpp" BACKGROUND_HPP_SHA)
  file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/fog.cpp" FOG_CPP_SHA)
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS fog.cpp)
  target_compile_definitions(th20_background_state_cpu_compare PRIVATE TH20_FOG_CPP_SHA="${FOG_CPP_SHA}")
  set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS state.cpp background.hpp)
  target_compile_definitions(th20_background_state_cpu_compare PRIVATE TH20_NATIVE_CORE_SHA256="unused" TH20_BACKGROUND_STATE_SHA="${BACKGROUND_STATE_SHA}" TH20_BACKGROUND_HPP_SHA="${BACKGROUND_HPP_SHA}")
endif()
if(TH20_BUILD_ORACLE)
  add_executable(th20_background_vm_cpu_compare vm_cpu_compare.cpp ../../src/binary.cpp ../../src/pe.cpp)
  target_link_libraries(th20_background_vm_cpu_compare PRIVATE th20_stage_background bcrypt)
  target_include_directories(th20_background_vm_cpu_compare PRIVATE ../../include ../../native_recovered)
  target_compile_options(th20_background_vm_cpu_compare PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
  target_link_options(th20_background_vm_cpu_compare PRIVATE /BASE:0x10000000 /DYNAMICBASE:NO)
  target_compile_definitions(th20_background_vm_cpu_compare PRIVATE TH20_NATIVE_CORE_SHA256="unused")
  foreach(pair IN ITEMS "VM|vm.cpp" "MOTION|camera_motion.cpp" "FOG|fog.cpp" "HPP|background.hpp" "ANM|../sprite_renderer/anm_vm.cpp" "MATH|../ecl_vm/math.cpp" "NATIVE|../../native_recovered/native_core.hpp")
    string(REPLACE "|" ";" parts "${pair}")
    list(GET parts 0 name)
    list(GET parts 1 path)
    file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/${path}" source_sha)
    set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${path}")
    target_compile_definitions(th20_background_vm_cpu_compare PRIVATE "TH20_BG_${name}_SHA=\"${source_sha}\"")
  endforeach()
endif()

target_sources(th20_stage_background PRIVATE object_draw.cpp object_projection.cpp)
