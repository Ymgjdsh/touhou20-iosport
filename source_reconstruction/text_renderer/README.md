The real `Renderer` (`0x1a360`, original AsciiInf at `0x5c0698`) and `Job` (`0x684`) have source constructors, destructors, six scheduler callbacks and actual shared ANM storage. `OwnedAnimation` invokes the recovered VM cleanup in the original member destruction order. The `0x5b66e0` allocator-bearing deferred task queue is unique source storage in `deferred_queue.cpp`.

`ascii.cpp` restores `0x46d300` layout/drawing for all fourteen font modes, including proportional widths, punctuation, newlines, alignment, rotation, shadow and accumulation of the temporary z coordinate. `format.cpp` supplies float, integer, padded integer, prefix/suffix, character, raw text and printf-style public entry points. `text.cpp` supplies lifetime/scheduling, persistent-job drawing and the `0x4a0aa0` loading animation, also bound to `gameplay::unrecovered::screen_transition`.

`bitmap.cpp` restores six Windows bitmap formats, DIB creation/fallback/release, alpha inversion and pixel outlining. `raster.cpp` restores `0x415830` CP932 conversion, all 22 font branches, GDI measurement/drawing, outline scale consumption, source cropping and deferred main-thread upload. The `0x4145d0` upload calls real D3D9 `GetSurfaceLevel`/`LockRect`, copies each original row width, releases the bitmap and raises the actual readiness byte before its optional completion callback. Normal production uses real OS/COM APIs.

`dynamic.cpp` restores `0x46c210`: reuse an expired matching job, clone a live job while sharing its readiness, or measure/allocate a new atlas rectangle and enqueue its raster task. Cancellation destroys the job before rasterization. `0x470b80` preserves the specimen's asymmetric texture-dimension divisors. Dynamic glyphs use `graphics_state.surface_animation` (`0x470670` returns `0x5c5880`), while ASCII uses `Renderer::animation_file`. `0x44ac20`/`0x44fff0` connect animation flags, packed texture ids, formatting and upload.

`cpu_validation.json` contains **38,681 passing checks, zero failures**. The original entry point is never called. All tests run in a separate x86 oracle, which resolves actual Win32 GDI/heap/locking imports and executes only selected original routines.

- Original baseline: 15,910 checks for line construction/compaction, ASCII layout, UVs, rectangle overlap, atlas placement, job registration, actual GDI text extent, float formatting and FPS scene/color behavior.
- 8,192 additional grouped-score checks cover signed values, each comma boundary, the five-group arithmetic rule, and complete line writes through the original 46d020 wrapper; test values stay within the game score range and exclude arithmetic-overflow/insufficient-buffer cases.
- 4,096 additional public-format checks compare all resulting Renderer bytes, including signed integer extremes, padding, prefixes/suffixes and printf formatting.
- 4,051 bitmap checks cover format-table results, DIB creation/fallback/initial pixels/release, random alpha conversion and outline pixels in 16/32-bit formats.
- 352 GDI raster/deferred-upload cases add 4,576 checks across all 22 font branches, spacing, outlines, font-offset selection and the 1024-pixel clamp. Original and source execute actual GDI calls; texture/surface COM boundaries use the same memory-backed fixture, comparing every raster/upload byte, readiness, rectangles and COM call counts.
- 1,024 rectangle/UV/matrix comparisons and 128 expired-cache reuse comparisons.
- 88 create/clone/raster/upload/cancel flows add 704 checks. They use original formatting, original job constructors/destructors and both real queue implementations. Animation self pointers are normalized only when they refer to their owning animation; strings are compared logically, and clone readiness is compared by presence. The cases exercise valid layouts and successful GDI operations.

Only the original two ASCII quad draw boundaries (`0x43f550`, `0x43f630`) are intercepted to record emitted VM arguments. This is not a real-GPU text rendering, arbitrary-allocation-failure, whole-Renderer lifetime, concurrency, or complete-game equivalence claim. The Renderer lifecycle control flow compiles, but whole-object startup/shutdown still needs integrated engine validation. Downstream scene/menu callers remain under reconstruction. No production function calls or embeds original machine code.
