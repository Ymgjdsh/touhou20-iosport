add_library(th20_screen_effect STATIC update.cpp environment.cpp lifecycle.cpp draw.cpp)
target_compile_features(th20_screen_effect PUBLIC cxx_std_20)
target_compile_options(th20_screen_effect PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
target_link_libraries(th20_screen_effect PUBLIC th20_sprite_renderer th20_runtime_core th20_platform_window th20_gameplay th20_runtime_state)
if(TH20_BUILD_ORACLE)
  add_executable(th20_screen_effect_cpu_compare cpu_compare.cpp ../../src/binary.cpp ../../src/pe.cpp)
  target_link_libraries(th20_screen_effect_cpu_compare PRIVATE th20_screen_effect bcrypt)
  target_include_directories(th20_screen_effect_cpu_compare PRIVATE ../../include ../../native_recovered)
  target_compile_options(th20_screen_effect_cpu_compare PRIVATE /W4 /utf-8 /fp:strict /arch:SSE2)
  target_link_options(th20_screen_effect_cpu_compare PRIVATE /BASE:0x10000000 /DYNAMICBASE:NO)
  target_compile_definitions(th20_screen_effect_cpu_compare PRIVATE TH20_NATIVE_CORE_SHA256="unused")
  foreach(pair IN ITEMS "UPDATE|update.cpp" "ENV|environment.cpp" "HPP|effect.hpp" "NATIVE|../../native_recovered/native_core.hpp" "RNG|../runtime_state/state.cpp" "LIFECYCLE|lifecycle.cpp" "DRAW|draw.cpp")
    string(REPLACE "|" ";" parts "${pair}")
    list(GET parts 0 name)
    list(GET parts 1 path)
    file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/${path}" source_sha)
    set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${path}")
    target_compile_definitions(th20_screen_effect_cpu_compare PRIVATE "TH20_SCREEN_${name}_SHA=\"${source_sha}\"")
  endforeach()
endif()
