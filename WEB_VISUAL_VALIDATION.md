# WASM 画面修复验证

记录日期：2026-09-19。范围为重新编译后的本地浏览器游戏页，人工观察菜单画面与浏览器错误输出。

## 本次修复

- **画面只占一部分：** HTML 画布初始尺寸与游戏申请的后备缓冲区不一致。WebGL 设备创建与重置时，现在按 `BackBufferWidth`、`BackBufferHeight` 同步画布；切换渲染目标时重置对应视口，并按当前目标处理坐标方向和清屏区域。
- **标题背景黑屏：** 浏览器异步载入动画资源时，标题初始化可能提前复制尚未完成的背景模板。标题状态零现在等待动画资源就绪后再初始化背景、推进计时。
- **背景与菜单合成：** WebGL 适配补充了纹理阶段的颜色/透明度运算、透明度测试、混合状态等，使恢复代码设置的绘制状态能够实际生效。
- **手动进关卡死：** 原加载函数在浏览器主线程等待资源工作完成，造成相互等待。Web 路径现在由协程逐帧推进，玩家动画模板就绪后才创建玩家，其余关卡资源完成后才启用战斗回调。
- **高刷新屏幕加速：** RAF 按显示器刷新调用，游戏逻辑现在按累积时间固定为每秒 60 帧；短按键会保留到下一次输入采样。

相关实现位于 `web/src/web_d3d9.cpp` 与 `source_reconstruction/title_system/frame.cpp`。

## 已确认结果

| 检查项 | 结果 |
| --- | --- |
| Canvas 实际尺寸 | 1280×960 |
| 游戏画面比例与覆盖范围 | 4:3，铺满游戏画布 |
| 标题主界面背景 | 已恢复显示 |
| 标题、难度、角色、石头选择菜单 | 可进入，显示正常 |
| 上述菜单检查期间浏览器 error / warning | 未观察到 |
| 手动从菜单进入第一关 | 加载状态 complete，场景 7，帧数持续递增 |
| 第一关画面（前一轮） | 背景地形、角色、敌人及 HUD 框架可见；当时未检查到分数文字缺失，本轮已修复并复测 |
| 输入 | 射击、右移、暂停得到画面响应 |
| 第一关逻辑更新速率 | 13.832 秒内 829 帧，约 59.93 帧/秒 |
| 第一关检查期间浏览器 error / warning | 未观察到 |

## 待完成检查

进关加载的复测已在重新编译后的同一版本完成。此次检查范围为第一关开场与暂停，未进行完整关卡通关。

本记录只覆盖上述画面问题和菜单路径。全部关卡、完整通关、音频，以及原版同输入同种子的逐帧一致性仍未验收，不能据此声称已实现完整 1:1 还原。

## 音频、分数与特效追加验证

2026-09-19，重新执行 `build_web.ps1` 后，在本机浏览器打开 `game.html?v=audio-effects3`。本节是本轮实测，优先于上面的前一轮描述。

- **BGM：** `Unlock` 现在更新正在播放的 PCM 环形缓冲区，按原通知边界回填；不会重新播放开头。标题音乐的实际浏览器游标连续到 1,160,832 帧（26.32 秒），写入 106 次，欠载 0；进入第一关正常换曲，关卡音乐继续到 1,971,712 帧（44.71 秒）、179 次写入、欠载 0。这些值是浏览器调度诊断，并非扬声器录音对照。
- **进关转场：** 实际截图捕捉到装备选择页上展开的原面板动画；记录 `starts=1`、转场第 16 帧、已绘制 16 次。进入关卡后收到关闭中断，推进到第 59 帧并结束，总绘制计数 146；没有用新制作的替代动画。
- **雾：** 第一关第 360 帧的实际截图可见远景被绿色雾覆盖、近景树木保留细节；后续背景颜色随原 STD 推进。
- **分数：** 右上角最高分 `1,000,000` 与当前分数 `0` 可见；战斗后依次观察到 `4,570`、`6,600`、`28,201`，逗号分组正常。
- **战斗效果：** 输入射击及 X 后，实际画面中观察到发光球、光柱与粒子；未单独对每一种玩家符卡做原版逐帧验收。
- **中 Boss 扭曲：** 自然运行到第一关第 5,160 帧，观察到中 Boss 环形效果和背景扭曲；原 17×17 网格运行诊断为半径 128、16 条带、更新计数 960。
- **正式 Boss 符卡：** 经正常游戏输入和续关推进到第一关 Boss，`spellId=0` 的「塵符『コズミックダスト』」实际显示专用背景、名称、倒计时、圆环、弹幕与背景扭曲；符卡年龄从 390 推进到 1,920 帧，网格半径 160、16 条带、更新计数到 4,620。符卡背景和效果句柄均非零。该记录证明本张符卡实际运行，不代表其他关卡符卡已经逐项验证。
- **浏览器错误：** 上述标题、进关与战斗检查中未观察到 console error / warning。

回归检查：

| 检查 | 结果与边界 |
| --- | --- |
| 分数文字 WASM/Node 检查 | 14 项通过，链接实际 `text_renderer/format.cpp` |
| WebGL 像素读回 | 浏览器实际执行 27/27 通过，所有 GL 错误码为 0；包含雾、透明度、混合、贴图方向、默认 backbuffer DESTALPHA 转场与 Present |
| PCM 环形缓冲测试 | 标题曲 106.2 秒、第一关曲 138.55 秒；共 10,793,475 帧、977 次回填，逐采样匹配原始 PCM，跨各自曲目循环点；使用模拟 AudioContext，不能替代实际扬声器验收 |

复现命令见 [web/tests/README.md](web/tests/README.md)，音频详细报告见 [reports/web_audio_stream_validation.json](reports/web_audio_stream_validation.json)，原效果调用覆盖及尚待对照项见 [web/EFFECTS_COVERAGE.md](web/EFFECTS_COVERAGE.md)。全部关卡、全部符卡和同种子逐帧对照仍未完成。
